node_modules/
dist/
.env
.env.production
.env.*.local
# (deploy/**/.env.production.example templates are NOT ignored — they have no secrets)

# Logs — never commit runtime logs
*.log
# Local scratch / one-off debug dumps & snapshots (kept out of the tree)
backend/scratch/
*.tmp
# QA proof screenshots (contain real employee PII) + capture scripts — local only
qa-proof/
# One-off auth audit script (contains dev test passwords) — local only
backend/scripts/authAudit.js
# View-only test user seed (contains hardcoded dev test password) — local only
backend/scripts/seedViewOnlyTestUser.js
# Local DB dumps / scratch (never commit — contains employee data + password hashes)
corehrms_dump.sql
*.sql
dump_err.txt

# Runtime integration config (per-install; may hold Google Maps/reCAPTCHA keys on the server)
backend/data/
# Build-artifact zips (never commit)
dist.zip

# Local backup of backend/.env taken during the email de-branding change (holds SMTP creds)
backend/.env.bak-*
# Browser QA screenshots (real employee names) + the capture script that creates a
# temporary login to take them — same reason as qa-proof/ above: local only.
screenshots/
backend/scripts/shotOvertimeScreen.js
backend/scripts/shotLeaveEligibility.js
# Ad-hoc Cashfree probes with a PRODUCTION client id/secret pasted inline. Never
# commit these — a payment-provider secret in git history cannot be un-published,
# it can only be rotated. Use backend/.env (CASHFREE_PROD_*) instead.
backend/scripts/cashfree_audit.js
backend/scripts/phase2_test.js
