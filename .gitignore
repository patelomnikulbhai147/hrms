node_modules/
dist/
.env
.env.production
.env.*.local
# (deploy/**/.env.production.example templates are NOT ignored — they have no secrets)

# Logs — never commit runtime logs
*.log
# Local scratch / one-off debug dumps & snapshots (kept out of the tree)
backend/scratch/
*.tmp
# QA proof screenshots (contain real employee PII) + capture scripts — local only
qa-proof/
# One-off auth audit script (contains dev test passwords) — local only
backend/scripts/authAudit.js