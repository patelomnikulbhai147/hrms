{
  "_note": "Employee fields are merged into the User profile on login. This is the underlying shape.",
  "employeeId": "string — official code",
  "firstName": "string",
  "lastName": "string",
  "email": "string|null",
  "phone": "string|null — mobile",
  "department": "string|null",
  "designation": "string|null",
  "companyId": "integer|null",
  "branchId": "integer|null",
  "status": "string — 'Active' counts as app-eligible"
}
