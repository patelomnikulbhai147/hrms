async function run() { for(let i = 0; i < 10; i++) { try { const res = await fetch('http://localhost:5000/api/employees', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ name: 'Test Emp ' + i, email: 'test' + i + '@gcri.in', companyId: 'c-gcri', department: 'Clinical', designation: 'Tester' }) }); const data = await res.json(); console.log('Created:', data.employeeId); } catch (e) { console.error('Error:', e); } } } run();